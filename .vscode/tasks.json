{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "make",
            "type": "shell",
            "command": "make clean && make",
            "options": {
            "cwd": "${workspaceFolder}/trabalho-pratico"
        },
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "problemMatcher": ["$gcc"],
            "detail": "Runs make clean and then make"
        }
    ]
}
